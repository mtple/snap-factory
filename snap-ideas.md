# Snap Ideas

Queue of snaps to build. Matt can add ideas here directly or tell FreeTurtle in Telegram.
FreeTurtle moves an idea from "Backlog" → "Queued" before building, then to "Built" after shipping.

## Queued (build these next)

_(none yet — pick from Backlog or generate fresh)_

## Built

_(none yet)_

## Backlog

- collaborative pixel art canvas using `cell_grid` (multi-select, persists across users via snap-turso)
- "rate this cast" — slider input + bar_chart results
- daily trivia question with `toggle_group` choice + reveal
- farcaster follower count guessing game (input + score history)
- music mood poll for /tortoise channel (toggle_group + bar_chart results)
- "would you rather" with `progress` bar showing live results
- tip calculator — two sliders (percent, split), text result
- countdown timer to a configurable event (input + progress)
- rock-paper-scissors against other users
- collaborative story builder — each user adds a word, growing text body
- coin flip with confetti effect on result
- pomodoro timer (slider for minutes, button to start, status text)
- color palette picker (cell_grid + named accent palette display)
- mini onchain price ticker (text + view_token action)
- swap helper (input amount, swap_token action)
- compose-cast templates ("today i learned...", "shipping update")
- mini leaderboard (item_group with items showing fid + score)
- guessing game: pick a number 1-100, slider input
- daily affirmation with the 8 accent colors rotating
- chess opening trainer (cell_grid 8x8, single-select, sequence of positions)
