{
  "name": "snap-factory",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Farcaster snaps built by Snap Wizard",
  "scripts": {
    "generate": "tsx scripts/generate-registry.ts",
    "prebuild": "npm run generate",
    "build": "tsc --noEmit",
    "predev": "npm run generate",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@farcaster/snap": "^1.21.0",
    "@farcaster/snap-hono": "^1.5.12",
    "@farcaster/snap-turso": "^1.0.21",
    "hono": "^4.12.12"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
